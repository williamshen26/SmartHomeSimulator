package com.ascii.simulator.device.util;

public class Commands {
    // initialize a static array of strings to store the commands
    public static final String[] COMMANDS = {
            "turn on the light",
            "turn off the light",
    };
}
